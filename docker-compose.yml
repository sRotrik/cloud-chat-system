version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    environment:
      - MONGO_URI=${MONGO_URI}
      - REDIS_URL=${REDIS_URL}
      - PORT=3001
    restart: unless-stoppedversion: '3.8'

services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    environment:
      - MONGO_URI=${MONGO_URI}
      - REDIS_URL=${REDIS_URL}
      - PORT=3001
    restart: unless-stopped